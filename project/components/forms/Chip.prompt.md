Pill chip for quick date-offset presets, label-size presets, and filters. Toggle via `selected`.

```jsx
<Chip selected={offset===5} onClick={() => setOffset(5)}>+5 days</Chip>
<Chip icon="plus">Custom</Chip>
```
