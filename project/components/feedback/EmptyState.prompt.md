Empty state for Labels, Templates and Remembered Labels. Always pair with a CTA.

```jsx
<EmptyState
  icon="tags"
  title="No labels yet"
  text="Create your first one — connect a printer and start designing."
  action={<Button icon="plus">New label</Button>}
/>
```
